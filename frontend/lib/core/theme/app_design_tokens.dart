import 'package:flutter/material.dart';

class AppPalette {
  const AppPalette._();

  static const cream = Color(0xFFFFF8F0);
  static const warmSurface = Color(0xFFFFFCF7);
  static const surface = Color(0xFFFFFFFF);
  static const graphite = Color(0xFF2C2724);
  static const graphiteMuted = Color(0xFF716861);
  static const terracotta = Color(0xFFC95F45);
  static const terracottaDark = Color(0xFF9D3F2D);
  static const sage = Color(0xFF6F8F72);
  static const sageDark = Color(0xFF49664B);
  static const lost = Color(0xFFB84235);
  static const found = Color(0xFF4F7A55);
  static const adoption = Color(0xFF87633F);
  static const line = Color(0xFFE8DED4);
}

class AppSpacing {
  const AppSpacing._();

  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 12.0;
  static const lg = 16.0;
  static const xl = 24.0;
  static const xxl = 32.0;
}

class AppRadii {
  const AppRadii._();

  static const sm = 8.0;
  static const md = 12.0;
  static const lg = 16.0;
  static const xl = 20.0;
}

class AppWidths {
  const AppWidths._();

  static const compact = 560.0;
  static const readable = 760.0;
  static const wide = 1080.0;
}
